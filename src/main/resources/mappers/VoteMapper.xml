<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devboard.warzone.mapper.VoteMapper">

    <select id="findVote" resultType="java.lang.Integer">
        SELECT direction
        FROM post_votes
        WHERE username = #{username}
          AND post_id = #{postId}
    </select>

    <insert id="insertVote">
        INSERT INTO post_votes (username, post_id, direction)
        VALUES (#{username}, #{postId}, #{direction})
    </insert>

    <update id="updateVote">
        UPDATE post_votes
        SET direction = #{direction}
        WHERE username = #{username}
          AND post_id = #{postId}
    </update>

    <delete id="deleteVote">
        DELETE
        FROM post_votes
        WHERE username = #{username}
          AND post_id = #{postId}
    </delete>

</mapper>